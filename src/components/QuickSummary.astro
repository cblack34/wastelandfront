---
export interface Props {
  title?: string;
  level?: 2 | 3 | 4 | 5 | 6;
}

const { title = "Quick Summary", level = 2 } = Astro.props;
const Heading = `h${level}`;
---

<div class="quick-summary">
  <Heading>{title}</Heading>
  <slot />
</div>